import React from 'react'
import styled from 'styled-components'

const Title = styled.a`
  color: red;
  &:hover {
    color: #ffffff;
  }
`
function Header(props) {
  return <Title>sd</Title>
}

export default Header
